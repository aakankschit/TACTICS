# Session Archive — June 2026

Archived from `research/STATUS.md` on 2026-09-10 when the active session log
exceeded 20 entries. Restore-style: append these back to STATUS Session Log
if specific narrative context is needed for an investigation.

- **2026-06-01 (cont.)**: Reagent score landscape figure + additivity-axis test. (1) **φ_int DEAD END** (card `d1468d13`): interaction-fraction-at-the-hits does not discriminate greedy-easy (quinazoline φ_int@100 0.551) from allocation-hard (amide_hybrid 0.549) — greedy failure is reagent-level mean-unreachability (select-then-evaluate), NOT score non-additivity; no additivity axis can positively explain quinazoline's easiness; extends 56dc09a3. `experiments/_debug/interaction_fraction_probe.py`. (2) **Coverage clamp clarified** (appended to framework card `66c29afe`): in `max(0, 1.5 + z_mean)` the max and the 1.5 are ONE mechanism — `max(0, z_mean)` is degenerate (all hit-hubs have z_mean < 0), so z* = −1.5 is the reachability reference (≈ top 7% by mean, Φ(−1.5)=0.067), justify descriptively + sensitivity, do not outcome-fit (n=8, reopens afa11eb2). De-clamping x to raw mean-quality was built + reverted (quinazoline lands mid-plot because its easiness is the ABSENCE of a failure mode, not an extreme mean); landscape stays the three greedy-failure-mode severities (origin = easy). (3) Renamed "SAR landscape" → "reagent score landscape" in `tutorials/manuscript_sar_plots.py` + `plots/reagent_score_landscape_map.pdf` + module title; broader SAR→reagent-score rename (ROCS section, other PDF filenames, marimo vars, `.py` filename) still PENDING. Combination of failure modes → method is Q1/Q2 of the framework (any failure → TACTICS; σ-character → TT-TS vs RWS). Resume: layer-2 within-component diagnostic (unchanged next step); optional explicit combination→method inset; warmup-reachability diagnostic → min_GMIC-from-warmup (line 185, Paper 3).
- **2026-06-01**: Docking-narrative figure + mechanism-framework consolidation. (1) Established the **three-mechanism method-winner framework** — coverage `max(0,1.5+z_mean)` / allocation `z_std×(1−top1)` / imbalance `max/min count` for TACTICS-vs-Legacy (any one sufficient); hidden-hub σ-character for TT-TS-vs-RWS — explicitly DESCRIPTIVE/post-hoc (constructed thresholds fit to 8 libs, not a classifier; run-both-take-max stands). (2) Reworked `tutorials/manuscript_sar_plots.py`: docking SAR landscape → 3-mechanism axes (x=coverage, y=allocation, edge-width=imbalance, fill=outcome); counterfactual bars recolored z(std)→−z(mean) mean-quality; hub-mechanism plots REMOVED; containment curve added then removed (→ two new DEAD ENDS: two-fraction-on-quinazoline + containment-curve). (3) Built **GMIC signal→decision→result diagnostic** (`plot_gmic_directed_exploration` in `diagnostic_plots.py` + thrombin cell loading `full_benchmark_5pct`); thrombin shown mean-accessible-but-estimation-limited. (4) 5 Heptabase cards (3 manuscript Section-Discussion: `66c29afe`/`ccf22197`/`548d7583`; 2 dead-end: `afa11eb2`/`56dc09a3`); card `5a862478` amended with the quinazoline caveat. Resume next session: build **layer-2** within-component diagnostic (`plot_adaptive_intensity`: RWS ema_relative_gmic→cats_multiplier, TT-TS disagreement_ema→heated_scale, RESULT = within-component sampling breadth), prototyping on a BALANCED library first.
