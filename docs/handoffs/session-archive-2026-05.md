# Session Archive — May 2026 (early)

Archived from `research/STATUS.md` on 2026-05-22 when the active session log
exceeded 20 entries. Restore-style: append these back to STATUS Session Log
if specific narrative context is needed for an investigation.

- **2026-05-01**: HYBRID benchmark setup + framework upgrade from regimes to three-axis SAR characterization. (1) **HYBRID parquet built and benchmark scaffolded.** Original `merge_full_hybrid.py` had a 2-column schema bug (HYBRID score files have 4 columns); ran corrected merge via polars on the mount → `data/scores/adenine_hybrid.parquet` (5,203,413 unique products, 28 valid amidines, score range -19.46 to 24.52, mean -8.16). Top hits dominated by amidine_009/006 and **aldehyde_137 (70/100)** + aldehyde_430 (20/100) — different SAR landscape from FRED (where amidine_003/035 dominate). Registered `adenine_hybrid` as discoverable library: added to `MINIMIZE_LIBRARIES`, `SCORE_TO_REAGENT_FOLDER` (→ adenine), `_CPU_SEC_PER_LIBRARY` in `examples/_benchmark_config.py`. Created `jobs/adenine_hybrid_benchmark.sub`. First submission (51579841) ran 0 trials because `scripts/stage_benchmark_data.sh` had a separate hardcoded folder-name override list missing `adenine_hybrid`; refactored stage script to use a single `REAGENT_FOLDER` map mirroring the Python config. Discovery now correctly stages 25 libraries. Two SARS-uncovered amidines (022, 029) are pure-failure batches (not chemically valid products — SMARTS template misapplies); `submit_missing_hybrid.sh` written but the 37 missing batches are these unrecoverable batches, so the campaign is effectively complete. (2) **Hidden-hub posterior-separation analysis on HYBRID.** aldehyde_137 has z(mean) = -1.04 (moderately separable) but z(std) = +6.33 (intrinsically very wide — much wider than FRED's amidine_003 at z(std) = +3.16). aldehyde_430 at z(mean) = -3.61 is a strongly mean-separable rescue (~20% of top-100). HYBRID classifies as Mixed Ia + Ib like FRED post-merge but with the critical component shifted from amidines to aldehydes; predicted method ranking: TT-TS or tied with TACTICS RWS (no bimodal-failure risk because ald137's wider std mechanically protects against warmup-narrow-posterior). (3) **Three-regime taxonomy → three-axis SAR-landscape framework rewrite.** User-driven: refactored `[METHOD]` card `a19b3878-be4e-4e57-b02e-605ff68eaea5` from regime taxonomy to a three-axis framework. **Axes**: (1) hidden-hub size = % of top-N in mean-uninformative reagents (z(mean) > -1.5); (2) μ-reachable share = % of top-N in mean-separable reagents (z(mean) < -1.5); (3) hidden-hub σ-robustness = z(std) of dominant hidden hub. Each axis maps to a specific TACTICS algorithmic mechanism activation. Three improvements over the regime taxonomy: continuous (not categorical), explicitly handles mixed Ia+Ib via independent axes 1 and 2, surfaces the σ-robustness axis (genuinely new) which explains FRED pre/post-merge role flip and HYBRID cross-receptor robustness. Library balance is upstream of the three axes: it influences axis 3 specifically because per-reagent warmup observations scale inversely with component size (small components → narrow empirical posteriors → low σ-robustness → higher TT-TS bimodal-failure risk). Card title auto-updated to `[METHOD] TACTICS RWS — Three-Axis SAR-Landscape Framework for Paper 1`. (4) **Pre-merge composition analysis confirms role-flip mechanism.** Reconstructed FRED pre-merge top-100 by filtering amidine_035 from post-merge parquet: 68 a003 + 5 strongly-mean-separable hits (a015 + a033) + 19 moderately separable + 8 hidden across other amidines. The role-flip threshold is not "any mean-separable rescue exists" but "substantial μ-reachable share emerges": 5% rescue terrain pre-merge was insufficient; 36% from amidine_035 alone post-merge is a 7× jump and tips the comparison. The bimodal-failure mechanism (warmup-narrow-posterior on hidden hub) is unchanged between pre and post merge — what changed is RWS reliability (a035 stabilizes the amidine GMIC signal, eliminating RWS pre-merge bimodal failures) and the size of TT-TS's exclusive σ-niche (68 → 49). Resume next session: integrate post-merge adenine into full benchmark, run per-library best-Legacy baseline post-hoc, draft manuscript Methods + Discussion using the (now-five) framing cards.
- **2026-05-01 (session 2)**: HYBRID benchmark execution + manuscript framing decisions + several runtime bugs found and fixed. (1) **HYBRID benchmark complete.** 100 trials, 5 methods × 20 reps × 1 query, 0 errors, 6.6 min runtime (`outputs/adenine_hybrid_benchmark/`). Top-100: TT-TS = 94.5 ± 5.1 (winner; **no bimodal failures**; 11/20 reps recover exactly 90 = 70 ald137 + 20 ald430 + 0 other; 9/20 reps recover exactly 100 = 70 + 20 + 10 tail-aldehyde — deterministic recovery of dominant hubs in every replicate); TACTICS RWS = 86.4 ± 14.3 (1 catastrophic [rep 15: recovery 26, only 1 ald137 hit, 902 ald137 evals vs 18,853 in successful reps] + 3 partial bimodal failures from aldehyde_137 GMIC mis-rotation); Legacy RWS = 84.0 ± 5.6; Greedy methods ~30. Best-TACTICS − best-Legacy = +10.5 pp. Cross-docking-pipeline framework validation confirmed: same chemistry, FRED axes (49%, 36%, +3.16) → RWS dominant; HYBRID axes (70%, 20%, +6.33) → TT-TS dominant. **Bimodal-failure mode migrated** from TT-TS-on-FRED (amidine_003 narrow-σ) to RWS-on-HYBRID (aldehyde_137 GMIC under-rotation) — confirms failure is library-and-receptor-specific, not method-specific. (2) **Manuscript framing refinements (user-driven).** Primary claim is **best-TACTICS beats best-Legacy across the board** (+0.4 to +53.8 pp range, only quinazoline ns); three-axis framework is **post-hoc mechanistic explanation, not prescriptive method-selection** (requires ground truth to compute axes); practical guidance is "use both TACTICS methods on any library, take the max — both individually robust, the better one substantially beats prior art on every benchmark library tested"; the *pair* of methods is robust because the bimodal-failure modes are anti-correlated. Adenine FRED + HYBRID positioned as **cross-docking-pipeline controlled experiment** (chemistry held constant, scoring varies — methodologically more defensible than "cross-receptor"); pre-merge adenine stays in supplementary at most. (3) **Bug fixes during HYBRID benchmark setup.** Discovered that `run_benchmark.py` resolved per-library `.smi` filenames and reactions.csv keys inconsistently from the reagent folder path. First-pass fix (this session) changed all 4 sites to use `folder_name`; that fix was wrong for hyphenated ROCS libraries and was superseded by the explicit `name_key` rule codified as CLAUDE.md Local Norm #11. (4) **macOS AppleDouble `._` files break SSHFS-mounted output_dir.** When `run_benchmark.py` writes temp parquets to `outputs/.../tmp_XXX/` on a macOS-SSHFS-mounted dir, macOS creates `._foo.parquet` xattr-companion files that downstream cleanup chokes on (`FileNotFoundError`). Workaround: use local-Mac output_dir (`~/tactics_local_outputs/`), then copy back via `cp -r` post-run. Alternative: `COPYFILE_DISABLE=1` env var disables AppleDouble creation for the process tree; works for keeping output_dir on the mount. Resume next session: integrate adenine post-merge + adenine_hybrid into `outputs/full_benchmark_ema_cats/`, recompute per-library best-Legacy baseline across the 25-library aggregate, draft manuscript Methods/Discussion paragraphs threading the across-the-board-win primary claim with three-axis framework as supporting mechanistic analysis.
